For more detailed instructions, please see doc/html/installation.html

To install the Semantic-Engine:

$ ./configure <options>
$ make
$ make install

To install the optional Perl bindings, 
first install the C++ toolkit (above), then:

$ cd pl
$ perl Makefile.PL
$ make
$ make test
$ make install

